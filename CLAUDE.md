# PEA 프로젝트 규칙

## 셸 명령 규칙
- 파일을 쓸 때 `cd`와 출력 리다이렉션(`>`, heredoc 포함)을 한 명령에 섞지 말 것.
- 항상 절대경로로 쓰거나, 프로젝트 루트에서 직접 명령을 실행할 것.
- git commit 메시지는 `cd "/abs/path" && git commit ...` 형태의 cd 체인으로 만들지 말 것.

## 셸·페인 설계 기준 (2026-07-02 확정)

이 프로젝트의 페이지 레이아웃은 아래 3패턴 중 하나로만 구성한다. 새 페이지를 만들 때 먼저 어느 패턴인지 판단하고 시작할 것. 상세 근거는 `docs/shell-decisions.md` 참고.

### 셸 3패턴
1. **콘텐츠 페이지** — 세로 스크롤 레이아웃 + 필요한 곳에만 내부 2단 섹션. 마스터-디테일(좌 리스트/우 상세 swap) 아님. 예: 기관소개, 연수안내 소개부.
2. **마스터-디테일** — 좌 = 리스트, 우 = 선택 항목 상세(swap). URL이 선택 상태의 단일 진실원천. **항목이 여러 개인 리스트에만** 적용. 예: 게시판(공지/FAQ), 마이페이지 신청목록.
3. **폼** — 신청 폼 = 모바일 우선 풀폼(데스크탑은 우측에 실시간 합계·요약 sticky 옵션). 문의 등 부수 폼 = 데스크탑 우선 + 모바일 quick-action 카드.

### 페인 역할 모델 (breakpoint별 배타 — 반드시 강제)
- **데스크탑 (마스터-디테일)**: L = 리스트 전용, R = 상세 전용. 같은 콘텐츠가 양쪽에 동시에 뜨면 안 됨. vaul OFF.
- **모바일 (마스터-디테일)**: L = 전체 리스트, 상세 = vaul 바텀시트 모달 전용. R 페인 없음.
- **vaul 바텀시트는 마스터-디테일에서만** 사용. 콘텐츠 페이지·폼은 모바일에서 컬럼이 세로로 스택된다(모달 아님).

### 하지 말 것
- 섹션이 few·fixed인 페이지(기관소개 등)에 마스터-디테일 스왑을 씌우지 말 것 → 좌측이 비거나 좌우 콘텐츠가 중복됨.
- `MasterDetail.tsx`(Phase 2a), `ScheduleCalendar.tsx`는 재작성/수정 금지.
